---
description: Settings page layout, tabs, Preferences section, AI Setup section, auto-save pattern, custom UI components
globs: ui/dashboard/settings.html,ui/static/js/dashboard-settings.js
alwaysApply: false
---

# Settings Page

## Tab Layout
File: `ui/dashboard/settings.html`

| Tab label | `data-section` | Section div ID | Notes |
|-----------|---------------|----------------|-------|
| Profile | `profile` | `profileSection` | Edit profile link + resume upload |
| AI Setup | `apiKeys` | `apiKeysSection` | API key cards + model selector |
| Preferences | `preferences` | `preferencesSection` | Workflow preferences, auto-saves |
| Privacy | `privacy` | `privacySection` | Export + data deletion |
| Account | `account` | `accountSection` | Password section (email users only) + Danger Zone |

Tab switching is handled by `showSection(sectionName)` via delegated click on `.settings-nav a[data-section]`. No `onclick` attributes.

**Tab switching clears the alert container** — `showSection()` empties `#alertContainer` on every tab change so notifications from one tab never carry over to another.

---

## AI Setup Tab (`apiKeysSection`)

Three mutually exclusive API key state cards (shown/hidden by `updateApiKeyStatusUI()`):

| Card ID | Shown when |
|---------|-----------|
| `serverKeyNotice` | `server_has_key && !has_user_key` |
| `byokNotice` | `!server_has_key && !has_user_key` — prompts user to add their own key |
| `userKeyNotice` | `has_user_key` — shows masked key preview + Remove button |

**`userKeyNotice` has two visual states** depending on `use_vertex_ai`:
- `use_vertex_ai=false`: cyan icon, subtitle `Active: XXXX…xxxx` (first 4 and last 4 of key), normal card border — key is being used
- `use_vertex_ai=true`: amber icon (`account-icon--amber`), amber card border (`account-card--warning`), subtitle explains key is stored but not used because the server handles AI internally — user can safely remove it

**Backend note:** `server_has_key` in the API response is `True` when either `settings.gemini_api_key` is set **OR** `use_vertex_ai=True`. Both mean the user never needs their own key.

**BYOK input form** (`byokNotice` card): separated from the header row by `border-top` + `pt-3 mt-1` on the `<form>`. Uses `input-group` (not `input-group-sm`). "Get API Key" button links to `https://aistudio.google.com/api-keys`.

**Model selector card** (`modelSelectorCard`):
- Shown **only** when `has_user_key === true` **AND** `use_vertex_ai === false`
- Hidden when the server uses Vertex AI (model is locked server-side)
- Populated by `loadModelPreference()` on show; saved by debounced `saveModelPreference()` (600 ms)
- "Saved ✓" indicator: `modelSavedIndicator` fades in on success
- Options are grouped by Gemini generation using `<optgroup>`

**Currently supported models**:
```html
<optgroup label="Gemini 3">
  <option value="gemini-3.5-flash">Best speed &amp; quality balance (recommended)</option>
  <option value="gemini-3.1-pro-preview">Most capable, advanced reasoning</option>
  <option value="gemini-3.1-flash-lite">Fastest &amp; lightest, lowest quota usage</option>
</optgroup>
<optgroup label="Gemini 2.5 (stable)">
  <option value="gemini-2.5-flash">Fast &amp; efficient</option>
  <option value="gemini-2.5-pro">Highly capable</option>
</optgroup>
```
**Dead models (do not re-add):** `gemini-3-flash-preview` (superseded by GA `gemini-3.5-flash`), `gemini-3.1-flash-lite-preview` (shut down 2026-05-25; use GA `gemini-3.1-flash-lite`), `gemini-3-pro-preview` (shut down 2026-03-26; redirects to `gemini-3.1-pro-preview`), `gemini-2.0-flash` / `gemini-2.0-flash-lite` (shut down 2026-06-01), `gemini-1.5-flash`, `gemini-1.5-pro` (not found / unsupported in v1beta).

`GET /api/v1/profile/api-key/status` response shape:
```json
{
  "has_user_key": bool,
  "has_api_key": bool,
  "server_has_key": bool,
  "use_vertex_ai": bool,
  "key_preview": "ABCD...wxyz" | null
}
```

---

## Preferences Tab (`preferencesSection`)

### Auto-Save Architecture
All preferences auto-save — **no Save button**. A "Saved ✓" indicator (`prefsSavedIndicator`) fades in for 2 s on success.

- Slider (`gateThresholdSlider`) fires `input` → updates display + `_schedulePreferencesSave()`
- Toggle (`autoGenerateDocsToggle`) fires `change` → `_schedulePreferencesSave()`
- Dropdowns (`coverLetterToneSelect`, `resumeLengthSelect`) fire `change` → `_schedulePreferencesSave()`
- All share a single 600 ms debounce timer (`_prefsSaveTimer`)

`_schedulePreferencesSave()` → debounce → `_persistPreferences()` → `PATCH /api/v1/profile/preferences`

### Current Preferences

| UI element | Field | Type | Default | Effect |
|-----------|-------|------|---------|--------|
| Range slider | `workflow_gate_threshold` | float 0–1 | 0.5 | Workflow stops early below this match score |
| Toggle | `auto_generate_documents` | bool | false | When off, user triggers document generation manually |
| Dropdown (inline) | `cover_letter_tone` | str | `professional` | Appends tone instruction to cover letter prompt |
| Dropdown (inline) | `resume_length` | str | `concise` | Appends verbosity instruction to resume advisor prompt |

### Preferences API
```
GET  /api/v1/profile/preferences   → ApplicationPreferencesResponse
PATCH /api/v1/profile/preferences  → ApplicationPreferencesResponse (partial update, upserts row)
```

Response shape:
```json
{
  "workflow_gate_threshold": 0.5,
  "auto_generate_documents": false,
  "cover_letter_tone": "professional",
  "resume_length": "concise",
  "preferred_model": null
}
```

---

## Custom UI Components

### Range Slider (`.pref-range`)
Custom-styled range input. The filled portion is driven by a CSS `--val` custom property updated in JS:
```javascript
function _updateSliderFill(slider) {
    const pct = ((parseInt(slider.value) - parseInt(slider.min))
               / (parseInt(slider.max)  - parseInt(slider.min))) * 100;
    slider.style.setProperty('--val', `${pct}%`);
}
```
Always call `_updateSliderFill(slider)` after setting `slider.value` programmatically.

### Custom Toggle (`.pref-toggle`)
Replaces Bootstrap `form-switch`. Structure:
```html
<label class="pref-toggle" aria-label="…">
  <input type="checkbox" id="myToggle" role="switch">
  <span class="pref-toggle-track"><span class="pref-toggle-thumb"></span></span>
</label>
```
- Unchecked: dark track (`rgba(255,255,255,0.12)`)
- Checked: gradient track (`var(--accent-gradient)`)
- Thumb: always white, translates 20 px on check

### Inline Dropdown (`.pref-select.pref-select-inline`)
Used for dropdowns that sit inline on the right of a card row:
```html
<div class="d-flex align-items-center gap-3">
  <div class="account-icon account-icon--cyan">…</div>
  <div class="flex-1"><div class="account-title">…</div>…</div>
  <select class="pref-select pref-select-inline" id="mySelect">…</select>
</div>
```
- `.pref-select` — full-width dark select with cyan arrow SVG; `color-scheme: dark` forces OS dropdown list to dark mode
- `.pref-select-inline` — `width: auto; min-width: 150px; flex-shrink: 0`
- `option { background-color: #0a0a0f }` — matches `--bg-primary`

### Full-Width Dropdown (`.pref-select` only)
Used in model selector and anywhere a dropdown needs full card width.

---

## Account Card Structure — CSS Classes (No inline `style=`)

All `.account-card` rows use CSS classes, never inline `style=` attributes. The CSP blocks inline styles.

### Account icon color variants (`.account-icon--{color}`)
Apply to the `.account-icon` div. The variant sets both the background and the icon color via a child `i` selector:

| Class | Use case |
|-------|---------|
| `.account-icon--cyan` | Default actions (profile edit, download, preferences, password) |
| `.account-icon--green` | Success/ready states (resume upload, server key active, help) |
| `.account-icon--purple` | AI/model features (preferred model, cover letter tone) |
| `.account-icon--amber` | Warning/setup-required states (BYOK notice, resume advice) |
| `.account-icon--red` | Destructive / danger zone |
| `.account-icon--google` | Google OAuth section |

```html
<!-- ✅ Correct -->
<div class="account-icon account-icon--cyan">
    <i class="fas fa-lock"></i>
</div>

<!-- ❌ Wrong — violates CSP style-src policy -->
<div class="account-icon" style="background: rgba(0, 212, 255, 0.12);">
    <i class="fas fa-lock" style="color: var(--accent-primary);"></i>
</div>
```

### Other account card classes

| Class | Purpose |
|-------|---------|
| `.flex-1` | Flex-grow text content div (replaces `style="flex: 1"`) |
| `.account-title--green` | Green title text (e.g. "Ready to Use") |
| `.account-title--red` | Red title text (e.g. "Danger Zone") |
| `.account-card--danger` | Red border on the danger zone card |
| `.account-card--warning` | Amber border — used when user key is stored but not in use (Vertex AI mode) |
| `.save-indicator` | Fade-in "Saved ✓" badge (`opacity: 0`, transitions to 1 via JS) |
| `.save-indicator--mt` | Same + `margin-top: 0.5rem` (for model saved indicator) |
| `.section-divider-row` | Header row with bottom border separator; child `.section-title` auto-loses its own border |
| `.threshold-display` | Large bold accent-colored percentage display |
| `.text-muted-xs` | Small muted helper text (`0.78rem`, `var(--text-muted)`) |

---

---

## Account Tab (`accountSection`)

### Account Type Rules — Strict "Either Email OR Google"
ApplyPilot does not support linking/unlinking OAuth accounts after registration. The UI enforces this:

- **Email-registered users** (`auth_method='local'`): see Password section + Danger Zone
- **Google-registered users** (`auth_method='google'`): see **only** the Danger Zone (no password section, no link/unlink button)

The old "Link Google Account" / "Unlink Google" section has been **permanently removed**. Do not re-add it.

### `userHasPassword` Module-Level Flag
`dashboard-settings.js` exposes `let userHasPassword = true;` at module scope. It is set by `loadGoogleAccountStatus()` which fetches `/api/v1/profile/` and checks `auth_method === 'local' || has_password`. The `passwordSection` div is shown/hidden based on this flag.

`deleteAccount()` reads `userHasPassword` to decide whether to show the password-confirmation step:
- `true` → second `showConfirm()` with `inputType: 'password'`
- `false` → skip directly to `performAccountDeletion('')`

### Danger Zone Buttons
| Button | Class | Action |
|--------|-------|--------|
| Clear Applications | `btn-danger` | Deletes all applications + AI results, keeps account/profile |
| Delete Account | `btn-danger` | Deletes the entire account |
| Export | `btn-primary` | Downloads data as JSON |

All three are default size (no `btn-sm`). All destructive actions use solid `btn-danger` (filled red), not `btn-outline-danger`.

### "Clear Applications" Flow
1. Check `GET /applications/stats/overview` first.
2. If `total_applications === 0` → `showAlert('You have no applications to clear.', 'info')` and return. No confirmation modal shown.
3. Otherwise → `showConfirm()` with a warning message (no "Type DELETE" input required).
4. On 429 response → display `"You have no applications to clear."` (rate-limited, no data to clear).

---

## Static Assets — Font Awesome (Self-Hosted)
Font Awesome is served locally from `ui/static/vendor/fontawesome/` — **not** from a CDN.

```
ui/static/vendor/fontawesome/
  css/all.min.css      ← referenced as /static/vendor/fontawesome/css/all.min.css
  webfonts/            ← fa-solid-900.woff2, fa-brands-400.woff2, etc.
```

`base.html` loads it with a plain `<link>` (not `asset_url()` — it doesn't go through the esbuild manifest). All child templates that previously loaded FA from cdnjs now use the same local path.

**Never re-add the cdnjs CDN link** — it is blocked in the local dev environment.
